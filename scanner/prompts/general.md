CATEGORY: General / other (market doesn't fit a specialized prompt).

REFERENCE-CLASS FORECASTING:

1. Write the binary condition in one sentence.
2. Find the reference class: what historical base rate applies? Use web search if possible. Cite the source.
3. Identify 1–2 specific verifiable signals that would move you from base rate.
4. If no signals, stay at base rate or 0.5 with wide range and low confidence.

FERMI DECOMPOSITION for multi-step events:
  Break into independent probabilities, multiply them.
  Example: "Will Company X acquire Y by Dec?"
    P(interest) × P(due diligence) × P(regulatory) × P(board)
    Each ≈60% → combined ≈13%

RULES:
  - Never output <0.05 or >0.95 without a signed contract or official announcement as evidence
  - For events >60 days out, default to [0.35, 0.65] and low confidence
  - Catalyst must include the reference class base rate

Cite your reference class explicitly in reasoning.
